// This component has been replaced by the inline ActionPanel in procurement-review-client.tsx
// Kept as an empty stub to prevent import errors from any cached references.

export default function RequirementDecision() {
  return null
}
